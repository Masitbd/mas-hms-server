import { Router } from 'express';
// import { JournalEntryController } from './journalEntry.controller';

const routes = Router();
// routes.get(
//   '/restore-journal',
//   JournalEntryController.restoreFailedJournalEntry
// );

export const JournalEntryRoutes = routes;
